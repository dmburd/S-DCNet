# @package _group_
part: 'A'
densmaps_gt_npz: './density_maps_part_A_*.npz'
# ^ shell pattern for the path to the files 
#   'density_maps_part_A_{train,test}.npz' 
num_intervals: 22
# ^ number of intervals for count values
#   (authors of S-DCNet use 22 for part_A)
xhp_gt_dmaps_dir: ./Test_Data/SH_partA_Density_map/test/gtdens/
# ^ path to the dir containing density maps as *.mat files 
#   (for part_A test set) from the repo
#   github.com/xhp-hust-2018-2011/S-DCNet
